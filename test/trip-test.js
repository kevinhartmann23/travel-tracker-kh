import {expect} from 'chai';

import Trip from '../src/admin.js'
// import {
//   travelers,
//   trips,
//   destinations
// } from 'test-data.js'

let newTrip;

describe('Trip', () => {
  beforeEach('instantiate a new agency class', () => {
    newTrip = new Trip()
  });

  it('should', () => {
    expect().to.equal()
  });

  it('should', () => {
    expect().to.equal()
  });

  it('should', () => {
    expect().to.equal()
  });

  it('should', () => {
    expect().to.equal()
  });

  it('should', () => {
    expect().to.equal()
  });

});
