import {expect} from 'chai';

import Admin from '../src/admin.js'
// import {
//   travelers,
//   trips,
//   destinations
// } from 'test-data.js'

let admin;

describe('Admin', () => {
  beforeEach('instantiate a new agency class', () => {
    admin = new Admin(travelers, trips, destinations)
  });

  it('should', () => {
    expect().to.equal()
  });

  it('should', () => {
    expect().to.equal()
  });

  it('should', () => {
    expect().to.equal()
  });

  it('should', () => {
    expect().to.equal()
  });

  it('should', () => {
    expect().to.equal()
  });

});
